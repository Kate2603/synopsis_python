8. Робота з модулями та створення віртуального оточення

1. Для чого використовується віртуальне оточення в Python?

-- Для збільшення швидкості виконання програми
-- Для автоматичного тестування коду
!- Для ізоляції залежностей проєкту
-- Для захисту коду від вірусів

Це правильна відповідь! Віртуальне оточення в Python використовується для ізоляції залежностей 
проєкту. Це дозволяє уникнути конфліктів між залежностями різних проєктів, запускаючи кожен 
проєкт у своєму власному ізольованому середовищі.

2. Яка команда використовується для створення нового віртуального оточення в Python?

-- python -m venv
!- python -m venv <назва_директорії>
-- python -m virtualenv <назва_директорії>
-- python -m pip install virtualenv

На жаль, не зовсім. 😞 Команда python -m venv <назва_директорії> використовується для 
створення нового віртуального оточення. Це ізолює залежності проєкту, уникаючи конфліктів 
з іншими проєктами чи глобальними налаштуваннями Python.

3. Який файл використовується для зазначення залежностей проєкту в Python?

-- dependencies.txt
-- config.py
-- setup.py
!- requirements.txt

Правильно! 😎 Файл requirements.txt використовується для вказівки залежностей проєкту в 
Python. Це дозволяє легко встановлювати необхідні бібліотеки та пакети за допомогою pip.

4. Яка команда використовується для активації віртуального оточення в Windows?

!- <назва_директорії>\Scripts\activate.bat
-- activate <назва_директорії>
-- source <назва_директорії>\Scripts\activate
-- python -m venv activate <назва_директорії>

Це правильна відповідь! Ви правильно обрали команду <назва_директорії>\Scripts\activate.bat 
для активації віртуального оточення в Windows. Ця команда активує віртуальне оточення, 
дозволяючи вам працювати із залежностями, ізольованими від глобального середовища.

5. Для чого в Python використовується pip?

-- Для запуску Python програм
!- Для встановлення та управління пакетами
-- Для компіляції Python коду
-- Для створення віртуальних оточень

Правильно! Pip є інструментом для встановлення та управління бібліотеками та залежностями в 
Python. Він використовується для зручного завантаження та встановлення пакетів із Python 
Package Index (PyPI) та інших репозиторіїв.

6. Яка команда використовується для деактивації віртуального оточення в Python?

!- deactivate
-- exit
-- close
-- stop

Правильно! 😊 Команда deactivate використовується для виходу з віртуального оточення в Python. 
Після її виконання, ви повертаєтесь до глобального середовища Python.

7. Що робить команда pip freeze?

-- Заморожує поточний стан системи
!- Показує список встановлених пакетів та їх версій
-- Створює віртуальне оточення
-- Деактивує віртуального оточення

Команда pip freeze не заморожує стан системи, не створює віртуальне оточення та не деактивує його. 
Її основна функція - відображати список встановлених пакетів та їх версій, що допомагає у 
керуванні залежностями проєкту.

8. Для чого в Python використовується файл __init__.py?

-- Для ініціалізації Python інтерпретатора
-- Для автоматичного запуску скрипту при старті проєкту
-- Для конфігурації віртуального оточення
!- Для вказівки, що директорія є Python пакетом

Це правильна відповідь! Файл __init__.py використовується для вказівки на те, що директорія 
містить Python пакет. Це дозволяє Python використовувати файли в цій директорії як модулі.

9. Яка основна мета використання модулів в Python?

-- Для зменшення використання оперативної пам'яті
!- Для організації та повторного використання коду
-- Для прискорення виконання програм
-- Для забезпечення взаємодії з операційною системою

Правильно! Основною метою модулів в Python є організація та повторне використання коду. Модулі 
дозволяють розбивати програми на менші частини, які легше підтримувати, тестувати та 
використовувати повторно в інших проєктах.

10. Яка команда використовується для встановлення пакетів з файлу requirements.txt?

-- pip install
!- pip install -r requirements.txt
-- pip setup requirements.txt
-- pip requirements install

Для встановлення пакетів із requirements.txt використовується команда pip install -r requirements.txt. 
Ця команда дозволяє встановити всі залежності, які перелічені у файлі, однією операцією.

11. Які переваги дає використання віртуального оточення у Python?

-- Підвищення швидкості виконання програм
-- Автоматичне видалення невикористаних пакетів
!- Ізоляція залежностей для конкретного проєкту
-- Захист від вірусів і шкідливого коду в пакетах

Правильно! Однією з ключових переваг віртуального оточення в Python є ізоляція залежностей 
для конкретного проєкту. Це дозволяє уникнути конфліктів між різними проєктами та забезпечує, 
що кожен проєкт має свої специфічні бібліотеки і версії пакетів.