7. Робота з файлами та обробка виключень

1. Яка команда в Python створює новий текстовий файл?

-- open('file.txt', 'r')
!- open('file.txt', 'w')
-- open('file.txt', 'x')
-- open('file.txt', 'a')

Правильно! Команда open('file.txt', 'w') відкриває файл для запису, створюючи новий 
файл, якщо він не існує.

2. Що робить метод write() у Python?

!- Записує рядок у файл
-- Читає рядок з файлу
-- Відкриває файл
-- Закриває файл

Правильно! Метод write() використовується для запису рядка у файл.

3. Як у Python відкрити файл в режимі тільки для читання?

!- open('file.txt', 'r')
-- open('file.txt', 'w')
-- open('file.txt', 'a')
-- open('file.txt', 'x')

Це правильна відповідь! 😊 Використання open('file.txt', 'r') в Python відкриває 
файл в режимі читання.

4. Що означає використання режиму 'a' при відкритті файлу у Python?

-- Читання файлу
-- Створення нового файлу
!- Додавання вмісту в кінець файлу
-- Відкриття файлу для читання та запису

Правильно! Режим 'a' використовується для додавання вмісту в кінець файлу, що вже існує.

5. Який режим відкриття файлу в Python дозволяє читати та додавати новий контент файл 
без його видалення, якщо він існує?

-- 'w+'
!- 'r+'
-- 'a'
-- 'x'

На жаль, це не зовсім так. 😟 Режим 'r+' дозволяє читати та додавати новий контент, в 
той час як 'w+' видаляє вміст файлу, якщо він існує, 'a' додає в кінець, а 'x' відкриває лише нові файли.

6. Який модуль у Python забезпечує високорівневі функції для роботи з файловою системою?

-- os
!- shutil
-- sys
-- fileinput

Правильно! Модуль shutil надає високорівневі функції для роботи з файловою системою, 
такі як копіювання файлів.

7. Яка функція в модулі pathlib в Python використовується для отримання повного шляху до файлу?

-- Path.cwd()
-- Path.home()
!- Path.absolute()
-- Path.relative_to()

Чудово, ця відповідь - правильна! Функція Path.absolute() використовується для отримання 
повного (абсолютного) шляху до файлу.

8. Для чого використовується метод seek() у Python?

!- Для переміщення курсору на певну позицію у файлі
-- Для пошуку певного тексту у файлі
-- Для перевірки чи файл існує на диску
-- Для заміни повної назви файлу

Правильно! Метод seek() використовується для переміщення курсору до певної позиції у файлі.

9. Яка функція використовується в Python для перевірки існування певного шляху або файлу?

-- os.path.exists()
!- pathlib.Path.exists()
-- shutil.file_exists()
-- fileinput.file_exists()

На жаль, це не зовсім правильна відповідь. 😟 Для перевірки існування файлу або шляху в 
Python використовується pathlib.Path.exists(), а не функції з інших модулів.

10. Який метод використовується у Python для читання всього вмісту файлу як одного рядка?

-- readable()
-- readline()
-- readlines()
!- read()

На жаль, це не зовсім правильна відповідь. 😟 Метод read() читає весь вміст файлу як 
один рядок, тоді як readlines() повертає список рядків, а readline() читає один рядок за раз.
